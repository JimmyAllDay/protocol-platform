export default async function handler(req, res) {
  //TODO: You need to update the mailingList route
  res.status(200).json({
    message:
      'This is the mailing list route. You still need to update the api.',
  });
}
